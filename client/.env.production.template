# GitHub Pages: https://rorocantcode.github.io/OpenFolio/
#
# No hosted API yet (no Render). Leave VITE_API_URL empty to verify the static
# build and asset paths only — sign-in and data will not work until you set a
# public API URL here and rebuild.
#
# VITE_API_URL=
#
# When you deploy the API later:
# VITE_API_URL=https://your-api.example.com
